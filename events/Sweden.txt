add_namespace = sweden

country_event = {
    id = sweden.1
    title = sweden.1.t
    desc = sweden.1.d
    picture = GFX_gustaf_v_death

    is_triggered_only = yes

trigger = {
        tag = SWE
        has_idea = gustaf_v
    }

    fire_only_once = yes

    mean_time_to_happen = {
        days = 50
    }

    option = {
        name = sweden.1.a
        add_national_unity = -0.05

        }
    }
}
